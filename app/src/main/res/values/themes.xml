<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Base.Theme.VideoPlayerApp" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Customize your dark theme here -->
        <item name="colorPrimary">#E50914</item>
        <item name="colorPrimaryVariant">#B20710</item>
        <item name="colorOnPrimary">@android:color/white</item>

        <!-- Status bar color -->
        <item name="android:statusBarColor">@android:color/black</item>
        <!-- Navigation bar color -->
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>

    <style name="Theme.VideoPlayerApp" parent="Base.Theme.VideoPlayerApp" />
</resources>